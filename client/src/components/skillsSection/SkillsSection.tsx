import SkillsList from "./SkillsList";

// styles
import "./SkillsSection.scss";

const SkillsSection = () => {
  return (
    <section id="skills">
      <div>
        <SkillsList />
      </div>
    </section>
  );
};

export default SkillsSection;
